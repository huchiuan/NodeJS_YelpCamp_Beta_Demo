    <% layout('layouts/boilerplate')%>
    <h1>所有的campgrounds</h1>
    <div>
        <a href="/campgrounds/new">新增campgrounds</a>
    </div>
    <ul>
            <% for (let campground of campgrounds){ %>
                <li><a href="/campgrounds/<%= campground.id %>"><%= campground.title %></a></li>
            <% } %>

    </ul>
